<% title "Edit Profile - Real Good Dog Rescue" %>
<% if current_user.id == @user.id %>
    <h1>Editing Profile</h1> 
<% else %>
    <h1>Editing <%= "#{@user.first_name} #{@user.last_name}" %></h1>
<% end %>

<%= render 'edit_form' %>

<%= link_to 'Show', @user %> |
<%= link_to "Change Password", edit_password_management_path(@user.id) %> |
<%= link_to 'Back', users_path %> 
