<% if logged_in? %>
    <nav class="navbar navbar-fixed-bottom">
        <ul class="nav navbar-nav">
            <li class="nav-item">
                <%= link_to "Profile", user_path(current_user) %>|<%= link_to "Settings", edit_user_path(@current_user) %>|<%= link_to "Edit Pages", static_pages_path %>|<%= link_to "Logout", logout_path, method: "delete" %>
            </li>
        </ul>
    </nav>
<% else %>
    <nav class="navbar navbar-fixed-bottom">
        <ul class="nav navbar-nav">
            <li class="nav-item">
                <%= link_to "Login", login_path %>
            </li>
        </ul>
    </nav>
<% end %>
