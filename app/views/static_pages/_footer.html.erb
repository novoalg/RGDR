<% if logged_in? %>
    <% if admin? %>
        <nav class="navbar fixed-bottom navbar-light">
            <ul class="nav navbar-nav">
                <li class="nav-item">
                    <%= link_to "Profile", user_path(current_user) %>|<%= link_to "Settings", edit_user_path(@current_user) %>|<%= link_to "Users Management", users_path %>|<%= link_to "Edit Pages", static_pages_path %>|<%= link_to "News Management", news_management_path %>|<%= link_to "Logout", logout_path, method: "delete" %>
                </li>
            </ul>
        </nav>
    <% elsif moderator? %>
        <nav class="navbar fixed-bottom navbar-light">
            <ul class="nav navbar-nav">
                <li class="nav-item">
                    <%= link_to "Profile", user_path(current_user) %>|<%= link_to "Settings", edit_user_path(@current_user) %>|<%= link_to "News Management", blogs_path%>|<%= link_to "Logout", logout_path, method: "delete" %>
                </li>
            </ul>
        </nav>
    <% else %>
        <nav class="navbar fixed-bottom navbar-light">
            <ul class="nav navbar-nav">
                <li class="nav-item">
                    <%= link_to "Profile", user_path(current_user) %>|<%= link_to "Settings", edit_user_path(@current_user) %>|<%= link_to "Logout", logout_path, method: "delete" %>
                </li>
            </ul>
        </nav>
    <% end %>
<% else %>
    <nav class="navbar fixed-bottom navbar-light" visibility="true">
        <ul class="nav navbar-nav">
            <li class="nav-item">
                <%= link_to "Login", login_path %>
            </li>
        </ul>
    </nav>
<% end %>
